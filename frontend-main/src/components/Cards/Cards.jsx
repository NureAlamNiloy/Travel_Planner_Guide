import { useLoaderData } from "react-router-dom";


export default function Cards({

}) {
  const fetchData =useLoaderData();
  // console.log(fetchData)
  return (
    <div>
      {/* {
        hotels.map(Card => console.log(Card))

      } */}
    
      
    </div>
  );
}
